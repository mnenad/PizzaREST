---
applications:
- name: pizza-poc
  memory: 512M
  host: pizza-poc
  domain: cfapps.io
  path: target/demo-0.0.1-SNAPSHOT.jar
  services:
  - datasource
  env:
    JAVA_OPTS : -Dspring.profiles.active=cloud
